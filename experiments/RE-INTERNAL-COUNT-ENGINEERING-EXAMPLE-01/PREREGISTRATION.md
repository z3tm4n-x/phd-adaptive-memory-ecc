# Статус предварительной фиксации

**Сравнительный эксперимент НЕ зарегистрирован и НЕ запущен.**

База проверена по origin/main: `7b83f643efb37541547d7d93a65ba2f43acd43fd`.
Ветка: `research/internal-count-engineering-example-01`.
Проверки `prerequisite_checks.py` — детерминированные адресные тесты
полного слова/задержки, а не подбор научных параметров или pilot policies.

Не выбраны новые W, H, закон среды, epsilon, временной/ресурсный контракт,
настройки конкурентов, размер эффекта и статистическая выборка.
В `config.json` неустановленные значения представлены null, не нулями.
Нельзя начинать production, трактуя этот файл как выполненную регистрацию.

Уже известны опубликованные результаты RES-003 и предыдущей GOES-поставки
`c0dd38ab3c01917e8e4bb165ecfae122b86dd9ae`; они не являются новой выборкой,
не доказывают перенос и не использованы для выбора новых интенсивностей.
При подготовке прочитаны принятый вывод, код выбора/наблюдения/error ledger
и относящиеся к применимости части отдельного SR. Повтор старых sweeps,
RADAR и новый GOES-прогон не выполнялись.

Для продолжения требуется разрешить область прикладного основания,
описанную в REPORT. После этого нужна полноценная фиксация одного объекта,
проверка исходного сертификата, strong-baseline классов и анализа **до**
новых сравнительных прогонов. Результат здесь не выдаётся за готовый
инженерный пример для статьи.
