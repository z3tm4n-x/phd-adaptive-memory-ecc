# Claims Registry

Здесь фиксируются ключевые утверждения (`CLM-xxx`), которые используются в научной аргументации.

Правило: существенный тезис должен быть явно классифицирован как `SOURCE`, `INFERENCE`, `ASSUMPTION` или `OWN RESULT` и иметь проверяемое основание.
